import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
	registerContainer: {
		width: "100%",
		height: "100vh",
		overflow: "hidden",
		borderRadius: 0
	}
}));

export default useStyles;
