import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
	banner: {
		maxWidth: "100%",
		borderRadius: 15
	},
	buttonBase: {
		borderRadius: 15
	}
}));

export default useStyles;
